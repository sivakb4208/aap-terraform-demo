---
- name: Create ServiceNow Incident for Drift
  hosts: localhost
  gather_facts: no
  become: no
  tasks:
    - name: Create ServiceNow Incident
      uri:
        url: "{{ sn_host }}/api/now/table/incident"
        method: POST
        user: "{{ sn_username }}"
        password: "{{ sn_password }}"
        force_basic_auth: yes
        body_format: json
        body:
          short_description: "Configuration Drift Detected on {{ target_host }}"
          description: "Automated drift detection found configuration issues on {{ target_host }}. AAP has attempted auto-remediation. Please verify compliance."
          impact: "2"
          urgency: "2"
          caller_id: "admin"
        status_code: 201
      register: snow_response

    - name: Print Incident Number
      debug:
        msg: "Incident created: {{ snow_response.json.result.number }}"
