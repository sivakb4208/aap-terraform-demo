---
- name: Drift Detection Rulebook
  hosts: all
  sources:
    - ansible.eda.range:
        limit: 5
        delay: 10

  rules:
    - name: Trigger remediation
      condition: event.i == 0
      action:
        run_job_template:
          name: "3 - Remediate Drift"
          organization: Default
